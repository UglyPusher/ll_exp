# Utilities

Small standalone programs for low-latency experiments.

- `affinity.cpp` - thread affinity experiments.
- `cache_locality.cpp` - locality and cache-friendly access checks.
- `numa.cpp` - early NUMA-related probes.
- `tp.cpp` - tiny standalone example.

These programs are built from the root `CMakeLists.txt`.
